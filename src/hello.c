/*Primeiro arquivo em linguagem C.
Para iniciar iremos importar a biblioteca atdio (std-standard(padrâo) io-input output (entrada e saída)). Esta biblioteca possui os comandos para interação com o teclado e monitor*/
#include <stdio.h>

/*Iniciar a estrutura do código para a mensagem hello word, usando a função main(principal), pois o compilador C irá procurar essa função para execução no nosso código.*/
int main(){
    printf("Hello World!\n");
    return 0;
}